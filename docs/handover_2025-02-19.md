# Handover — Feb 19, 2025

## 1. Session Summary

**Goal**: Fix Adhkar screen bugs, remove useless English memorization mode, activate Follow Along Recitation feature.

**Accomplished**:
- Fixed Adhkar animation glitch (blank frames between cards)
- Fixed tab bar overlap on Adhkar screen (now uses `<Modal>`)
- Redesigned Adhkar completion screen (premium celebration with particles, stats, Arabic praise)
- Fixed Adhkar time estimate (now realistic based on total taps)
- Removed entire Memorization Mode (useless English recall)
- Added Follow Along ear button to surah headers
- Installed `@jamsch/expo-speech-recognition` + configured plugin in `app.json`
- Fixed Adhkar crash: "Rendered more hooks than during the previous render" (moved `useMemo` above early returns)
- Softened morning Adhkar gradient colors for readability

**Left to do** (see Section 5):
- Fix Follow Along button icon (shows `?` — wrong icon set)
- Prebuild to get speech recognition native module working
- Test Follow Along on device

## 2. Files Changed

| Action | File | What Changed |
|--------|------|-------------|
| MODIFY | `app/surah/[id].tsx` | Removed MemorizationMode imports/state/buttons. Added Follow Along ear button to hero + sticky headers. |
| MODIFY | `app/_layout.tsx` | Removed `<MemorizationProvider>` wrapper |
| MODIFY | `app/(tabs)/index.tsx` | Wrapped Adhkar screen in `<Modal>` to fix tab bar overlap |
| MODIFY | `src/presentation/screens/AdhkarScreen.tsx` | Fixed hooks crash (moved useMemo above returns), softened morning gradient, redesigned completion screen, fixed time estimate, fixed animation glitch |
| MODIFY | `app.json` | Added `speechRecognitionPermission` + `microphonePermission` props to `@jamsch/expo-speech-recognition` plugin config |
| MODIFY | `package.json` | Added `@jamsch/expo-speech-recognition` dependency |

## 3. Current State

**Works**:
- Adhkar intro screen, session flow, completion celebration ✅
- Morning/evening toggle ✅
- Surah reader, audio, notes, bookmarks ✅
- TypeScript compiles clean ✅

**Broken/Needs Fix**:
- **Follow Along button shows `?` icon** — `ear-outline` is NOT a valid MaterialCommunityIcons name (react-native-paper uses MaterialCommunityIcons, not Ionicons). Need to use a valid icon like `ear-hearing` or `microphone-outline` or a custom approach.
- **Speech recognition shows "not available"** — `@jamsch/expo-speech-recognition` was installed to `package.json` but the user hasn't done `npx expo prebuild --clean --platform ios` + Xcode rebuild yet. The native module `ExpoSpeechRecognition` doesn't exist in the current binary.

## 4. Critical Context

### Architecture
- `VoiceRecognitionService.ts` — Singleton, uses `NativeModules.ExpoSpeechRecognition` directly (NOT the npm import). Has `isSpeechModuleAvailable()` graceful check.
- `MatchingService.ts` — Sliding window Arabic text matching. Normalizes diacritics, alef variations, teh marbuta. Allows 2 noise words.
- `useVoiceFollowAlong.ts` — Hook that orchestrates voice recognition + matching. Already fully wired into `surah/[id].tsx` with auto-scroll, verse highlighting, save modal.
- `VoiceFollowAlongOverlay.tsx` — Bottom overlay showing listening status + transcript.

### Tricky Bugs
- **Hooks violation in AdhkarScreen**: `useMemo` was placed BETWEEN two early returns (`phase === 'intro'` and `phase === 'complete'`). When phase changed, different number of hooks ran → crash. Fix: moved all hooks above any early return.
- **`_layout.tsx` nesting**: When removing `<MemorizationProvider>`, easy to get duplicate closing tags. Verify JSX tree is balanced.

### User Preferences
- User strongly prefers features grounded in real-world Quranic practice, not gimmicks
- User values premium, polished UI — not MVP/basic
- User wants Follow Along to be karaoke-style (verse-level highlighting as they recite)
- Morning Adhkar colors should be soft/readable, not intense amber

## 5. Recommended Next Steps

1. **Fix the Follow Along icon** — In `app/surah/[id].tsx`, change `icon={followAlong.isActive ? 'ear' : 'ear-outline'}` to a valid MaterialCommunityIcons name. The icon `ear-hearing` works, or use `microphone-outline`/`microphone`. Both hero header (around line 660) and sticky header (around line 730) need fixing.

2. **Prebuild + test speech recognition**:
   ```bash
   npx expo prebuild --clean --platform ios
   cd ios && pod install && cd ..
   ```
   Then build from Xcode and test on physical device (speech recognition doesn't work in simulator).

3. **Test Follow Along end-to-end**: Open a surah → tap ear icon → recite Arabic → verify verses highlight green → verify auto-scroll → tap stop → verify save modal appears.

4. **Consider Phase 2 of Follow Along**: Hidden text memorization mode (blur verses, reveal word-by-word as user recites from memory). See `implementation_plan.md` in artifacts.

## 6. Commands to Run

```bash
# Verify TypeScript
npx tsc --noEmit

# Prebuild (REQUIRED — native module added)
npx expo prebuild --clean --platform ios
cd ios && pod install && cd ..

# Then build from Xcode (Product → Archive or Cmd+B for debug)
```
